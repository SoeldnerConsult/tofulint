version: "2"

run:
  timeout: 5m


formatters:
  enable:
    - gci
    - gofmt

linters:
  exclusions:
    rules:
      - path: 'opentofu/.*'
        linters:
          - gosimple
        text: 'ST1005:'
